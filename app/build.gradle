plugins {
    id 'duckasteroid'
    id 'application'
}
description "Command line utilities for working with CDB files"

application {
    mainClassName = 'cdb.Main'
}

dependencies {
    implementation project(':library')
}
